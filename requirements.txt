# 基础依赖
pandas>=1.5.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.11.0
requests>=2.28.0
python-dotenv>=0.19.0
PyYAML>=6.0
fredapi>=0.4.3
yfinance>=0.2.40
# 报告导出依赖
markdown>=3.4.0
playwright>=1.30.0

# 可选依赖（用于 PDF 生成）
# weasyprint>=57.0
# wkhtmltopdf (需要单独安装)

# 可选：SCP 同步报告到云服务器
paramiko>=3.0.0
scp>=0.14.0

# 可选：AI 叙事层（通义千问 / OpenAI / Gemini）
# dashscope>=1.14.0   # 阿里通义千问
# openai>=1.0.0       # OpenAI
# google-generativeai  # Gemini
